pipeline {
    agent any
    stages {
        stage('Build') {
            // Build code
        }

        stage('Test') {
            // Test cases to run
        }

        stage('Deploy') {
            steps {
                sh "echo hello world"
            }
        }
    }
}