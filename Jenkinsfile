pipeline {
    agent any
    stages {
        stage('Build') {
            // Build code
        }

        stage('Test') {
            // Test cases to run
        }

        stage('Deploy') {
            // Deploy to GCP VM
        }
    }
}